import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  resendButton: {
    marginTop: 10,
    width: '80%',
  },
  resendText: {
    color: '#007BFF',
    textAlign: 'center',
    fontSize: 16,
  },
});