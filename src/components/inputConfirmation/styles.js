import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  inputFieldContainer: {
    flex: 1,
    margin: 5,
  },
  inputField: {
    height: 50,
    borderColor: '#ddd',
    borderWidth: 1,
    borderRadius: 5,
    textAlign: 'center',
    fontSize: 18,
    backgroundColor: '#fff',
  },
});